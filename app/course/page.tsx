import React, { FC } from "react";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "EdTech | Course",
  description: "Course",
};

const page: FC = () => {
  return <div>page</div>;
};

export default page;
